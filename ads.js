/* Pre-print ad slot config.
   MONETIZATION: to run a real ad network in the before-you-print dialog,
   paste the network's embed HTML into NETWORK_AD_HTML below (e.g. an
   AdSense display unit's <ins> + <script> snippet). Check your network's
   placement policy for interstitials first - Google's own "vignette" ads,
   for example, must be served through their auto-ads system.

   While NETWORK_AD_HTML is empty, the slot shows house ads: two picks from
   the prep-gear page, using the affiliate links from products.js. */

var NETWORK_AD_HTML = "";
