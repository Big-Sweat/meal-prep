{
  "name": "mise-android",
  "version": "1.0.0",
  "private": true,
  "description": "Mise — Android app. Capacitor shell around the Mise web app; the web files in the repo root are the single source of truth.",
  "scripts": {
    "sync": "node scripts/sync-web.js && cap sync android",
    "open": "cap open android",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/share": "^8.0.1"
  }
}
