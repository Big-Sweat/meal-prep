<?xml version="1.0" encoding="utf-8"?>
<!-- Flour-paper counter with a strip of manila tape across it — the site's
     signature element. Runs past the edges on purpose so the launcher's mask
     crops it like real tape. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <path
        android:fillColor="#F5F2EA"
        android:pathData="M0,0h108v108h-108z" />

    <group
        android:pivotX="54"
        android:pivotY="54"
        android:rotation="-6">
        <path
            android:fillColor="#EAD9A8"
            android:pathData="M-10,40h128v28h-128z" />
    </group>
</vector>
