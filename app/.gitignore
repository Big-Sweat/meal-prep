# generated by `npm run sync` — the repo root is the source of truth
www/

node_modules/

# Android Studio / IntelliJ editor state — machine-specific
.idea/
*.iml

# Android build output
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/release/
android/capacitor-cordova-android-plugins/
*.apk
*.aab
*.keystore
*.jks

# release signing — secrets, never commit
keystore.properties
android/keystore.properties
*.jks
*.keystore

# Kotlin compiler daemon session state — throwaway build artifact
.kotlin/
android/.kotlin/
